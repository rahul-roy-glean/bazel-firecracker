apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: firecracker-runner
type: Opaque
stringData:
  # Replace with your Cloud SQL private IP or service DNS name.
  host: "10.0.0.10"
  username: "postgres"
  password: "REPLACE_ME"
---
apiVersion: v1
kind: Secret
metadata:
  name: github-credentials
  namespace: firecracker-runner
type: Opaque
stringData:
  # Must match the GitHub webhook secret configured in the repo/org webhook.
  webhook_secret: "REPLACE_ME"


